# Copy to .env.local for local dev. On Vercel, set under Project Settings → Environment Variables.

# Your deployed Fastify backend URL (must match FRONTEND_ORIGIN's counterpart CORS setup on the backend)
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
